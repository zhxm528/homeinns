# 解决支持自查询的酒店下拉框待选项为空的问题
- 问题在于 useEffect 中 loadHotelOptions() 和 fetchData() 并行执行，fetchData 可能在选项列表加载完成前就执行，未等待选项加载完成。
- 修复：确保 loadHotelOptions 先完成，再执行 fetchData。建议使用 await loadHotelOptions() 确保选项列表先加载完成，然后再执行查询。同时，即使查询失败，选项列表也会从 API 响应中获取并显示。